language: node_js
sudo: required
node_js:
  - 9.5.0
cache: 
  yarn: true
  directories:
    - node_modules
install:
  - yarn install
  - npm i -g firebase-tools
script:
  - yarn build:bsb
  - yarn build:webpack
  - echo "Deploying !"
after-success:
  - firebase deploy --token $FIREBASE_TOKEN
